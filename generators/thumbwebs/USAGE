Description:
    Creates a migration that adds thumbwebs_id fields to the given model

Example:
    ./script/generate thumbwebs user

    This will create:
        db/migrate/TIMESTAMP_add_thumbwebs_fields_to_user
